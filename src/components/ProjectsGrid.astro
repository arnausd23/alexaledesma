---

---

<div class="bento-grid carousel-content relative">
  <div class="col-0 scroll-up">
    <div id="projectDetail"></div>
    <img src="https://images.pexels.com/photos/5386829/pexels-photo-5386829.jpeg" alt="">
    <img src="https://images.pexels.com/photos/18273081/pexels-photo-18273081/free-photo-of-islandia-mar-paisaje-montanas.jpeg" alt="">
    <img src="https://images.pexels.com/photos/18624573/pexels-photo-18624573/free-photo-of-mujer-calle-tarde-precioso.jpeg">
  </div>
  <div class="col-1 scroll-up">
    <img src="https://images.unsplash.com/photo-1703701008461-ce64767019d3?w=800&auto=format&fit=crop&q=60&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxlZGl0b3JpYWwtZmVlZHw1OHx8fGVufDB8fHx8fA%3D%3D" alt="">
    <img src="https://images.unsplash.com/photo-1704319236961-3cc376bdcf3f?w=800&auto=format&fit=crop&q=60&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxlZGl0b3JpYWwtZmVlZHwyOHx8fGVufDB8fHx8fA%3D%3D" alt="">
    <img src="https://images.pexels.com/photos/16453280/pexels-photo-16453280/free-photo-of-pajaro-sentado-arbol-aguila.jpeg" alt="">
  </div>
  <div class="col-2 scroll-up">
    <img src="https://images.unsplash.com/photo-1704509284035-d46a34f642c3?w=800&auto=format&fit=crop&q=60&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxlZGl0b3JpYWwtZmVlZHw1fHx8ZW58MHx8fHx8" alt="">
    <img src="https://images.unsplash.com/photo-1704500355467-88351c0797c3?w=800&auto=format&fit=crop&q=60&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxlZGl0b3JpYWwtZmVlZHwyM3x8fGVufDB8fHx8fA%3D%3D" alt="">
    <img src="https://images.unsplash.com/photo-1704168370831-b7f450dadeb0?w=800&auto=format&fit=crop&q=60&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxlZGl0b3JpYWwtZmVlZHwxM3x8fGVufDB8fHx8fA%3D%3D" alt="">
  </div>
</div>

<style>
  .scroll-up {
    animation: scrolling-up 30s linear infinite;
  }

  .scroll-up:hover {
    animation-play-state: paused;
  }

  @keyframes scrolling-up {
    0% { transform: translateY(0); }
    100% { transform: translateY(var(--translate-distance)); }
  }

  @media (max-width: 600px) {
    .scroll-up {
      animation: none;
    }
  }

  /* GRID */
  .bento-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    grid-template-rows: 100%;
    gap: 30px;
  }

  .col-0 {
    grid-area: 1/1;
    display: flex;
    flex-direction: column;
    gap: 30px;
    --translate-distance: 0;
  }
  .col-1 {
    grid-area: 1/2;
    display: flex;
    flex-direction: column;
    gap: 30px;
    --translate-distance: 0;
  }
  .col-2 {
    grid-area: 1/3;
    display: flex;
    flex-direction: column;
    gap: 30px;
    --translate-distance: 0;
  }

  #projectDetail {
    position: absolute;
    width: 100px;
    height: 100px;
    background-color: red;
    z-index: 999;
  }
  #projectDetail {
    display: none;
  }
  .col-0:hover #projectDetail {
    display: block;
  }
</style>

<script>
  Promise.all(Array.from(document.images).filter(img => !img.complete).map(img => new Promise(resolve => { img.onload = img.onerror = resolve; }))).then(() => {
    const pxToVW = (px: number) => {
      return px * (100 / [document.documentElement.clientWidth])
    }
    
    const cols = document.getElementsByClassName("scroll-up");
  
    for (let index = 0; index < cols.length; index++) {
      const col = cols[index];
      const childs = col?.childElementCount;
      const colImgs = col.getElementsByTagName("img")

      const gapBetweenImgs = (colImgs.length) * 30 // gap of grid in px
      let colHeight = gapBetweenImgs;
      for (let index = 0; index < colImgs.length; index++) {
        colHeight += colImgs[index].height
      }

      col.style.setProperty("--translate-distance", `-${pxToVW(colHeight)}vw`);
      for (let index = 0; index < childs; index++) {
        console.log(col.children[index])
        col.appendChild(col.children[index].cloneNode(true));
      }
    }
});
</script>

<script>
  // JS para el div en hover de un proyecto
  //todo: el id sale duplicado el duplicar el elemento, como hay un overflow:hidden no sale
  const myDiv = document.getElementById('projectDetail');

  if (myDiv && myDiv.style.display !== 'none') {
    function move(e) {
      const x = e.pageX;
      const y = e.pageY;
      
      console.log(x, y)
      myDiv.style.left = x  + 'px';
      myDiv.style.top = y  + 'px';
      // myDiv.style.left = x - 50 + 'px';
      // myDiv.style.top = y - 50 + 'px';
    }
    
    document.addEventListener('mousemove', e => {
      move(e)
    });
    // aplicar todo este JS a elemento que se muestre solo en :hover, sino display: none
  }
</script>